#include <stdio.h>

int main() {
    // Declare variables
    char name[] = "Mehtab";  // Use an array to store a string
    int age = 19;
    float height = 5.8;

    // Print information
    printf("My name is %s and my age is %d\nand my height is %f\n", name, age, height);

    return 0;
}
